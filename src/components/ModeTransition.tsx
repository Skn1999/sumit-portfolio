import { useMode } from "@/contexts/ModeContext";
import { motion, AnimatePresence } from "framer-motion";

const ModeTransition = () => {
  // Temporarily disable overlay to keep site background pure white
  return null;
};

export default ModeTransition;

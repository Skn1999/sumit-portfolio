---
slug: "optmyzr-dashboard-migration"
title: "Optmyzr: Migrating Legacy Ad Analytics to a Config-Driven React Architecture"
tagline: "Replacing a legacy Backbone.js and jQuery stack with a modular component architecture to eliminate critical UI rendering bottlenecks."
date: "2026-01-15"
type: "design"
featured: true
cover:
  filename: "cover.png"
  alt: "Migrated React dashboard showing core widget architecture layout"
tech:
  - "React"
  - "TypeScript"
  - "Redux Toolkit"
  - "Performance Engineering"
  - "Component Design"
metric: "Dashboard load times reduced from 8-10s to <1s; zero core state changes required to support new ad platforms"
links:
  # prototype: "https://github.com/skn1999/sumit-portfolio"
summary: "A production migration of Optmyzr's two highest-traffic, data-dense analytics applications. Rebuilt legacy corporate dashboards into an optimized React configuration pipeline that handles complex state propagation and multi-platform scaling cleanly."
roles:
  - "Frontend Engineering"
  - "UI Architecture"
order: 2
draft: true
---
import { ProjectImageAsset } from "@/components/ui/project-image-asset"
import {
  ImpactMetricBanner,
  ImpactMetric,
  CaseStudySplit,
  CaseStudyText,
  CaseStudyMedia,
  EngineeringCallout
} from "@/components/projects/CaseStudyLayout";

## Overview

Optmyzr is a PPC management SaaS platform serving digital advertising agencies handling high-volume accounts across Google, Microsoft, and Amazon Ads. As part of the Core team, I owned the frontend migration of the platform's two highest-traffic, data-dense applications: the **Account Dashboard** and the **PPC Comparison Tool**. 

The mission was critical: eliminate a mounting mountain of technical debt and transition the system into a performant, modular architecture where supporting new ad networks required zero core code modifications.

---

## The Problem: Systemic UI Collapse under Enterprise Scale

The original dashboard infrastructure relied on Backbone.js, manual jQuery DOM manipulation, and server-side templates. When enterprise accounts pulled massive datasets from external ad APIs, the interface structurally buckled:

* **Severe Performance Deficit:** High-spend account dashboards took between **8 to 10 seconds** to reach an interactive state, severely damaging user retention.
* **Cascade Re-rendering Loops:** Changing a single metric parameter or date range in one component triggered un-scoped DOM update loops across every single sibling widget on the page.
* **Rigid Maintenance Blockers:** Adding a single new ad format (such as Amazon Ads) required manual engineering interventions across the entire base data layer, view controllers, and stylesheets.

---

## Engineering & Architecture Decisions

Fixing these bottlenecks required decoupling our UI rendering orchestration from the underlying data source models. I engineered the recovery through three targeted architectural shifts:

### 1. Breaking the Reactivity Cascade via Scoped Redux Slices

<CaseStudySplit>
  <CaseStudyText>
    To halt global re-rendering loops across complex multi-widget interfaces, I established rigid data boundaries:

    * **Domain State Isolation:** Segmented data pipelines into independent, localized Redux slices to prevent un-scoped global notifications from forcing sibling recalculations.
    * **Memoized Selector Filters:** Configured components to subscribe exclusively to the granular dataset properties required for execution, dropping unneeded sibling evaluation passes to zero.
    * **Referential Stability:** Enforced strict rendering filters by wrapping UI modules in `React.memo` and securing downstream triggers with `useMemo` and `useCallback`.
  </CaseStudyText>
  <CaseStudyMedia>
    <ProjectImageAsset src="optmyzr-dashboard-migration/cascade.png" alt="Redux State Isolation Barrier comparing global data collapse versus scoped slice filters" />
  </CaseStudyMedia>
</CaseStudySplit>

### 2. Viewport-Driven Data Hydration
Enterprise accounts regularly process hundreds of simultaneous metrics, warning matrices, and data layers. Flooding the browser's main thread with this data instantly froze the UI tab. 

I restructured the delivery flow into a progressive, asynchronous asset loading model:
1. The layout engine renders lightweight core frame skeletons immediately upon mounting.
2. High-priority alert modules paint initial visible critical text chunks instantly.
3. Heavy graphical plots, charts, and deep tabular data break downs defer calculations, running asynchronously only as their bounding containers break the viewport threshold.
### 3. Config-Driven Component Architecture

<CaseStudySplit className="max-w-[120ch]">
  <CaseStudyText>
    To completely isolate interface layout variations from the underlying core application code, I designed a metadata-driven configuration factory that instantiates components dynamically at runtime based on strict JSON definitions.

    On app initialization, the route engine parses the account type directly from the application URL context. The core orchestrator fetches the corresponding metadata configuration matrix, iterates through the map objects, and instantiates components cleanly without touching the primary engineering base.
  </CaseStudyText>
  <CaseStudyMedia>
    <ProjectImageAsset src="optmyzr-dashboard-migration/dynamic-widget-hydration.png" alt="Config-driven development workflow for dynamic widget instantiation and webpage hydration. Allows for flexible, maintainable UI composition as well as faster loads for the users." />
  </CaseStudyMedia>
</CaseStudySplit>

<details className="not-prose my-8 rounded-xl border border-border bg-muted/50 p-5 md:p-6">
  <summary className="cursor-pointer font-mono font-semibold uppercase tracking-wider text-primary code-snippet-heading">
    Sample Code Snippet
  </summary>
  <div className="mt-4 text-sm leading-relaxed text-foreground md:text-base">
    ```typescript
    // Abstracted representation of the plug-and-render interface mapping
    import React from 'react';
    import { widgetRegistry } from './registry';

    interface WidgetConfig {
      componentPath: string;
      title: string;
      props: Record<string, any>;
    }

    export const WidgetRenderer: React.FC<{ config: WidgetConfig[] }> = ({ config }) => {
      return (
        <>
          {config.map(({ componentPath, props, title }) => {
            const TargetWidget = widgetRegistry[componentPath];
            if (!TargetWidget) return null;
            
            return <TargetWidget key={title} {...props} />;
          })}
        </>
      );
    };
    ```
  </div>
</details>

## Results & Product Impact

- **90% Load Acceleration**: Core application initialization and Time-to-Interactive (TTI) dropped permanently to under 1 second.
- **Zero-Engine Maintenance**: Integrating entirely new advertising platforms moved from an extensive, multi-day core feature rewrite to a fast, risk-free configuration-authoring checklist.  
- **Reference System Blueprints**: The core dynamic configuration framework was adopted as the official reference architecture for all upcoming multi-account management tooling.  

## Implementation Stack

Languages & Core State: React, TypeScript, Redux Toolkit, React.memo   
Data Integration: Firebase Realtime Database, Google Ads API, Microsoft Advertising API   
Profiling & Tooling: Chrome DevTools (Performance Profiling), Webpack Bundle Optimization   